weather:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: weather_user
      password: weather_pass
      dbname: weather_db
      schema: public
      threads: 1
